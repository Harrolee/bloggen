import { noteData } from "../data/noteData.json" assert { type: "json" };

export function showNotes(key) {
  return nodeData`.${key}`;
}

// command line options:

// sync:
// add all files to notes dir
// add all file data to json
// add:
// add file to notes dir
// add metadata of file to json.
